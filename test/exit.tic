Declare {number} code
Print "Enter an exit code: "
Input code

Print "In five seconds this program will exit with exit code "
Print code
PrintLine "..."

Declare count = 5

# the while loops are pretty straight forward
While count > 0 Repeat
    PrintLine count
    Set count = count - 1
    Sleep 1
EndWhile

Exit code