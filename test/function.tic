# A simple function

# "Function myfunc Takes {number} x, {bool} y Does"
# is also correct

Function myfunc Takes
	{number} x, {bool} y
Does
	PrintLine "myfunc called!"
	Print "x: "
	PrintLine x

	If y Then
		PrintLine "y is true!"
	Else
		PrintLine "y is false!"
	EndIf

EndFunction

Declare {bool} mybool = true
Declare mynum = 1
Input mynum

Call myfunc With mynum, mybool