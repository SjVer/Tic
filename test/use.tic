# "function.tic" is in the same directory as this script, that's why this syntax can be used.
# Otherwise the script should be given as a path in a string
Use function

# myfunc is defined in "function.tic" and thus imported.
Call myfunc

# mysecondfunc takes a number and a bool
Call mysecondfunc With 2.5, true