# An example script in Tic

Function myfunc Does
	PrintLine "myfunc called!"
EndFunction

Label MyLabel
Call myfunc
Exit 0

Function mysecondfunc Takes
	{number} x, {bool} y
Does
	PrintLine "mysecondfunc called!"
	Print "x: "
	PrintLine x

	If y Then
		PrintLine "y is true!"
	Else
		PrintLine "y is false!"
	EndIf
EndFunction

Label START # the script will start here

Declare mybool = true
Declare {number} mynum

Print "Enter a number: "
Input mynum

Call mysecondfunc With mynum, mybool
GoTo MyLabel

